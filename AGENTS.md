# PG Group - Employee Suggestion Portal (ESP)

This project is deployed on **Vercel** (`pggroup-esp-1b2d96b1.vercel.app`) with **Supabase** backend.
Ensure all environment variables (Supabase, SMTP, WhatsApp) are properly configured in Vercel Project Settings.

